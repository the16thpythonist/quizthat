/* eslint-disable */

export class Player {
    name: string;
    color: string;

    constructor(
        name: string = '',
        color: string = '#000000'
    ) {
        this.name = name;
        this.color = color
    }
}